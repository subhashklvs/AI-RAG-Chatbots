# Secrets — NEVER commit
.env

# Vector store (re-created by ingest.py)
chroma_db/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Streamlit
# (Make sure to commit .streamlit/config.toml so the theme applies on deployment)
